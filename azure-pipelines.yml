trigger:
- master

pr:
- master

pool:
  vmImage: 'ubuntu-latest'

steps:
 - script: curl https://github.com/sstephenson/bats/archive/v0.4.0.zip -o bats.zip && unzip bats.zip -d bats && cp -r bats /bin 
   displayName: 'Install bats'
   workingDirectory: '/tmp' 
 
 - script: command -v bats 
   displayName: 'check bats'

 - script: bash -c 'shopt -s globstar; shellcheck ./bin/tfvm'
   displayName: 'Run shellcheck'

